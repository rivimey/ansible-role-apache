{{ apache_global_vhost_settings }}

{% if global_server_status %}
{# fact set if any vhost has serverstatus = yes #}
<IfModule mod_status.c>
  ExtendedStatus On
  <IfModule mod_proxy.c>
    # Show Proxy LoadBalancer status
    ProxyStatus On
  </IfModule>
</IfModule>
{% endif %}
